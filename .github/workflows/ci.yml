name: Mon premier pipeline
on:
  push:

jobs:
  bonjour:
    runs-on: ubuntu-latest
    steps:
      - name: Dire bonjour
        run: echo "Bonjour depuis GitHub Actions !"

  aurevoir:
    runs-on: ubuntu-latest
    steps:
      - name: Dire au revoir
        run: echo "Au revoir depuis GitHub Actions !"
